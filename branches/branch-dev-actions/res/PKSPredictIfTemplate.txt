{%for 1 to depth %}    {%/for%}if ({%if predicate %}{{predicate}} && ({%/if%}{%for tokenKind in lookaheadSet%}LA(1) == {{tokenKind.name}}{%if currentLoop.currentIndex != last %} || {%/if%}{%/for%}{%if predicate %}){%/if%}) {
