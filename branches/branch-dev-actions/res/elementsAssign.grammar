@start = stat;

stat = assign dot | list semi;

assign = list eq list;

list = lbracket elements rbracket { LOG(LT(1)); };
elements = element (comma element)*;
element = Number | list;

lbracket = '['; // fence
rbracket = ']'!;
comma = ','!;

eq = '=';

dot = '.';
semi = ';';