@start = stat;

stat = assign dot | list semi;

assign = list eq list;

list =      lbracket { LOG(@"after rbracket"); }
            elements { LOG(@"after elements"); }
            rbracket { LOG(@"after rbracket"); };

elements = element (comma element)*;
element = Number | list;

lbracket = '['; // fence
rbracket = ']'!;
comma = ','!;

eq = '=';

dot = '.';
semi = ';';