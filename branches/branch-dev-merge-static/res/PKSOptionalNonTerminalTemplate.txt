{%for 1 to depth %}    {%/for%}if (([self predicts:{%for tokenKind in lookaheadSet%}{{tokenKind.name}}, {%if currentLoop.currentIndex == last %}0]{%/if%}{%/for%}) && ([self speculate:^{ {{ifTest}}}])) {
{{childString}}{%for 1 to depth %}    {%/for%}}
