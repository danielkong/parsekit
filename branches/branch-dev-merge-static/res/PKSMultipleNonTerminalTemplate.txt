{%for 1 to depth %}    {%/for%}do {
{{childString}}{%for 1 to depth %}    {%/for%}} while (([self predictsAny:{%for tokenKind in lookaheadSet%}{{tokenKind.name}}, {%if currentLoop.currentIndex == last %}0]{%/if%}{%/for%}) && ([self speculate:^{ {{ifTest}}}]));
