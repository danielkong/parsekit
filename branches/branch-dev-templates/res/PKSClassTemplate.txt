enum {
{%for userType in tokenUserTypes %}    {{userType}}{%if 0 = currentLoop.currentIndex %} = {{startIndex}}{%/if%},
{%/for%}};

@interface {{className}} : PKSParser

@end

@implementation {{className}}

- (id)init {
	self = [super init]
	if (self) {
		
	}
	return self;
}

- (void)dealloc {
	
	[super dealloc];
}
{{methods}}
@end