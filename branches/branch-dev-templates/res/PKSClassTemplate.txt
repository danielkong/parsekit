typedef enum {
    TOKEN_TYPE_BUILTIN_EOF = -1,
{%for userType in tokenUserTypes %}    {{userType}} = {{currentLoop.currentIndex}},
{%/for%}};

@interface {{className}} : PKSParser

@end

@implementation {{className}}

- (id)init {
	self = [super init]
	if (self) {
		
	}
	return self;
}

- (void)dealloc {
	
	[super dealloc];
}
{{methods}}
@end